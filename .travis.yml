language: c
before_install:
    - sudo apt-get install -y valgrind
script:
    - cd Debug
    - make clean
    - make
    - valgrind --log-fd=1 ./Base 
    - valgrind --log-file=leak.out ./Base 
    - grep "LEAK SUMMARY" leak.out

